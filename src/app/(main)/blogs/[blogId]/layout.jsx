// app/blogs/layout.js
export const metadata = {
  title: "Blog Details",
  description: "Blog Details",
};


const layout = ({children}) => {
    return (
        <>
        {children}
        </>
    );
};

export default layout;